# Stock Price Movement Prediction 📈

This project uses historical stock data to predict whether the stock price will go up or down the next day.

## Features
- Real data from Yahoo Finance (`yfinance`)
- Feature engineering (SMA, price changes)
- K-Nearest Neighbors with Grid Search
- Model evaluation & accuracy

## Installation
```bash
pip install -r requirements.txt
